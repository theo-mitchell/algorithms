# A = [2, 3, 4]
# B = [3, 4, 2]


# def isRotation(A, B):

#     for index, value in enumerate(B):
#         if A[0] == value:
#             for index1, value1 in enumerate(A[1:]):
                
           
    
SELECT  
    id, SUM (balance), COUNT(balance)
FROM Transactions
GROUP BY [ID] DESC

// How to resolve merge conflicts in git? 
// diffrence between soft and hard links in cmd 
// how to merge branches in git            
